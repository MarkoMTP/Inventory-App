<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Add New Genre</title>
    <style>
      /* General Styles */
      body {
        font-family: "Arial", sans-serif;
        margin: 0;
        padding: 0;
        background-color: #f7f9fc; /* Light background color */
        color: #333;
        text-align: center;
      }

      h1 {
        font-size: 3rem; /* Increased font size */
        color: #34495e; /* Darker color for sophistication */
        margin-top: 50px;
        margin-bottom: 20px;
      }

      form {
        background-color: #ffffff; /* White background for the form */
        padding: 30px;
        margin: 0 auto;
        max-width: 500px;
        border-radius: 8px; /* Rounded corners */
        box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1); /* Soft shadow */
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      label {
        font-size: 1.2rem;
        margin-bottom: 10px;
        color: #2c3e50;
        font-weight: 500;
        text-align: left;
        width: 100%;
      }

      input[type="text"] {
        padding: 12px 20px;
        font-size: 1.1em;
        margin-bottom: 20px;
        width: 100%;
        border: 2px solid #ccc;
        border-radius: 5px;
        transition: border-color 0.3s ease;
      }

      input[type="text"]:focus {
        border-color: #3498db; /* Highlight border on focus */
        outline: none;
      }

      button {
        background-color: #2ecc71; /* Green color for Add Genre button */
        color: white;
        padding: 14px 30px;
        border: none;
        border-radius: 50px; /* Rounded button */
        cursor: pointer;
        font-size: 1.2em;
        transition: background-color 0.3s ease, transform 0.3s ease;
      }

      button:hover {
        background-color: #27ae60; /* Darker green on hover */
        transform: translateY(-3px);
      }

      button:focus {
        outline: none;
      }

      /* Link to Back */
      a {
        color: #8e44ad;
        font-size: 1.1em;
        margin-top: 20px;
        display: inline-block;
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
      }

      a:hover {
        color: #9b59b6; /* Purple color for hover */
      }

      /* Centering the whole page content */
      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
        text-align: center;
      }

      /* Media Query for Small Screens */
      @media (max-width: 768px) {
        h1 {
          font-size: 2.4rem;
        }

        label {
          font-size: 1.1rem;
        }

        input[type="text"] {
          font-size: 1em;
        }

        button {
          font-size: 1.1em;
        }
      }
    </style>
  </head>
  <body>
    <h1>Add a New Genre</h1>
    <form action="/genres" method="POST">
      <label for="name">Genre Name:</label>
      <input type="text" id="name" name="name" required />
      <button type="submit">Add Genre</button>
    </form>
  </body>
</html>
